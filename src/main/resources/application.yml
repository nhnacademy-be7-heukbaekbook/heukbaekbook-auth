spring:
  application:
    name: heukbaekbook-auth
  config:
    import: optional:configserver:http://localhost:9000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://admin:1234@localhost:8082/eureka